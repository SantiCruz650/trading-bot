# MCrypto Frontend Configuration

# URL base del backend (FastAPI). 
# En desarrollo local suele ser http://localhost:8000/api
# En producción, debe ser la URL donde se haya desplegado el backend.
# IMPORTANTE: No puede ser el mismo dominio que Netlify.
VITE_API_URL=http://localhost:8000/api
